services:
  api:
    image: hermes3d-api:local
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./storage:/app/storage
      - ./configs:/app/configs:ro
    ports:
      - "8080:8080"
